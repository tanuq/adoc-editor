services:
  adoc-editor:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ${DOCS_DIR:-./docs}:/workspace/mounted
    environment:
      - WORKSPACE=/workspace/mounted
      - PORT=3000
